# PixelCraft · Aseprite · Resprite 对比与锐评

> 生成时间：随项目迭代更新
> 目的：客观对比本项目（PixelCraft）与行业标杆 Aseprite、移动端商业软件 Resprite 的差异，并给出对本项目的犀利复盘与改进优先级。

## 〇、最新进展（1.0.6.x）

本文件初稿里标记为“缺失/没做完”的条目，多数已经落地，阅读下文时请以此为准：

| 当时的批评 | 现状 |
|---|---|
| 无新手引导、发现性差 | ✅ 已有 41 步模块化高亮引导（`src/app/guide.ts`），含真操作演示，可重放、可按模块跳过 |
| 洋葱皮基本、无拖拽重排 | ✅ 洋葱皮有前后帧数/不透明度/着色/**循环环绕配色**；图层与帧号均可长按拖拽重排 |
| 无多选、无帧时长 | ✅ 时间轴**帧多选**（批量复制/删除/统一时长）；每帧独立时长并参与 GIF 导出 |
| 自动保存塞 localStorage、>4MB 跳过 | ✅ 改为 **IndexedDB**（上限 32MB），后台立即落盘，可手动保存/清除 |
| 性能：每次笔迹全量重算 | ⚠️ 部分解决：**脏矩形合成 + 局部重绘 + rAF 合并**，笔迹只重算改动区域；仍未做 overlay 笔迹层与 Web Worker |
| 无键盘快捷键 | ❌ 仍未做（移动端优先） |
| 生态：只读不写 `.aseprite`、无 tilemap、无脚本 | ❌ 仍未做 |
| 导出格式少 | ✅ 增加**帧范围**选择、分图层批量导出、调色板 `.gpl` 导出 |
| 调色板功能薄 | ✅ 增加去重 / 按色相·明度排序 / 合并预设色板 / 长按色块全局换色；仍无量化与抖动 |
| 设置不可搜索、无法备份 | ✅ 设置支持搜索、单项恢复默认、整套设置导出/导入 JSON |
| 返回键行为粗糙 | ✅ 返回手势逐层关闭（弹窗→面板→浮动球→引导），再按一次才退出 |
| 特效只有一键式，参数不可调 | ✅ 描边（宽度/内·外·居中/颜色）、投影（偏移/颜色/不透明度）、模糊（半径）都有参数弹窗并**实时预览** |
| 油漆桶只能纯色填充 | ✅ 新增渐变模式：前景→背景径向渐变，颗粒可选 RGB 平滑 / 2×2 / 4×4 / 8×8 |
| 无喷枪 | ✅ 新增喷枪工具：随机点大小区间与密度可调，支持对称/选区 |
| 一次只能开一张画布 | ✅ 多画布无限空间：标题栏聚焦/拖动/双击适配，画布球两页管理（保存/色相/导出/适配/平铺），每张画布独立撤销栈，多预览框按需出现，默认空白工程 |
| 撤销历史切画布就丢 | ✅ 每张画布独立历史，工程文件逐张保存 |
| 平铺只能 3×3 重复/镜像 | ✅ 改为 关闭 / 横向 / 竖向 / 九宫格（移除镜像） |
| 渐变只能按点击点径向 | ✅ 按住拖动定方向与长度、松手落笔（Aseprite 风格线性渐变） |


---

## 一、三款工具定位

| 维度 | **PixelCraft（本项目）** | **Aseprite** | **Resprite** |
|---|---|---|---|
| 平台 | 移动 WebView / 网页（自研无 Gradle APK） | 桌面 Win/macOS/Linux（Steam） | 移动 iOS / Android App |
| 收费 | 免费开源 | 付费（Steam / 官网） | 订阅制 |
| 定位 | 触屏像素画快速创作 | 行业标准·全能编辑器 | 移动端商业像素画「工作室」 |
| 目标用户 | 手机随手画、自用 / 移植作品 | 专业、独立游戏美术 | 移动端创作者 |

**一句话**：Aseprite 是「正统」，Resprite 是「移动端商业竞品」，PixelCraft 是「把 Aseprite 像素算法搬到触屏的免费移植/自用工具」。

---

## 二、功能维度对比

| 功能 | PixelCraft | Aseprite | Resprite |
|---|---|---|---|
| 笔刷 / 形状 | 铅笔·橡皮·填充·形状·魔棒；笔刷为 Aseprite 半径栅格，椭圆/圆用 **Zingl 源码 1:1** | 同类且更全面（压力、纹理、自定义笔刷） | 移动端主流齐全 |
| 对称 | **可旋转对称轴（0/45/90/135）+ 锁定 + 四向**，比 Aseprite 固定镜像更灵活 | 竖/横/对角镜像 | 基础镜像 |
| 变换 | 移动 / 旋转 / 缩放（Aseprite 仿射复刻） | 更多（含变换细节） | 有 |
| 图层 / 帧 | 基础图层 + 帧 | **帧标签、循环区间、多选、成组、裁剪/参考/遮罩层** | 较全 |
| 时间轴 | 洋葱皮完整（前后帧数/不透明度/着色/循环环绕）、图层与帧拖拽重排、**帧多选批量操作**、四种循环模式；仍无帧标签、无缩略图 | 成熟 | 较全 |
| 调色板 | 色板 + HSV + 取色 | **量化、降色、抖动、图像取色板** | 有 |
| 特效 | 描边 / 投影 / 外发光 / 反色 / 灰度 / 居中 / 智能裁剪 | 滤镜更全（扭曲/旋转/色相分离等） | 一般 |
| 导出 | PNG / 精灵表 / GIF / 调色板 `.gpl` / 分图层，**支持帧范围** | **ASE 格式、WebP、APNG、JSON 数据、tilemap** | PNG/GIF |
| 互操作 | **自定义 `.pxc`，无法读写 `.aseprite`** | 事实标准 | 一般 |
| 脚本 / 自动化 | **无** | **Lua 脚本 + CLI** | 无 |
| Tilemap | **无** | **图块层 / 图块集** | 无 |
| 桌面体验 | **无快捷键、无右键菜单、无多文档** | 成熟 | 移动端为主 |
| 上手 / 发现性 | 触屏球/停靠/圆片 + **41 步模块化高亮引导（真操作演示）** | 按钮多但成熟 | 移动端顺手 |

---

## 三、锐评：PixelCraft 的痛点与缺点

### 1. 性能是最大瓶颈（已缓解，未根治）
原本 `onMove → repaint() → buildComposite()` 每次移动都全量重算整帧所有图层 + 洋葱皮。
现已改为 **脏矩形增量渲染**：`Stroke.takeDirty()` 报告改动区域 → `composeRectInto` 只重合成该区域（cel 上传也限定到矩形）→ 视图只重绘对应屏幕矩形，且 `repaint()` 用 rAF 合并（一帧一次）。洋葱皮幽灵帧与选区染色图都做了缓存。
- 128×128 / 256×256 已明显顺滑；
- 仍未做：笔迹画到独立 overlay 层（松手才落盘）、Web Worker 导出、大画布（1024² × 多图层）下的压力测试。

> 下一步要彻底解决，得把“正在画的笔迹”从 cel 里拿出来，单独画在视口 overlay 上。

### 2. 生态锁死（互操作硬伤）
- 自研 `.pxc`，**不能读/写 `.aseprite`**；
- 导入仅 PNG / GIF / 精灵表；无 WebP / APNG / JSON 数据导出；
- 与团队、社区、既有 Aseprite 项目协作基本不可能，只能「自娱自乐」。

### 3. 桌面 / 网页端体验缺失
- `App.tsx` 中**零 `keydown`**：无 Ctrl+Z/Y、无 B/E/G/P 切工具、无 Ctrl+S、无 `[]` 调笔刷、无空格平移；
- 整个 UI 是**触屏范式**（魔法球 / 停靠区 / 圆片 / 长按），鼠标操作别扭；
- 大量功能埋在天球 / 子菜单里，**发现性极差、无新手引导**。

### 4. 功能「有但没做完」
- 变换：无透视、无浮层独立编辑、松手即落盘；
- 帧 / 层：已有拖拽重排与**帧多选批量操作**，仍**无帧标签 / 循环区间、无图层成组、无时间轴缩略图**；
- 调色板：已有去重 / 排序 / 合并 / 全局换色，仍无量化 / 降色 / 抖动 / 图像取色板；
- 无 tilemap、无 Lua 脚本、无参考层 / 裁剪层。

### 5. 数据安全（已改善，仍有缺口）
- ✅ 自动保存迁到 **IndexedDB**（上限 32MB），后台切换立即落盘，设置页可查看时间/大小并手动保存或清除；
- ✅ 设置可整体导出/导入 JSON，换机不丢配置；
- ❌ 工程仍只有“当前文档 + 一份自动保存”，**无多版本备份 / 崩溃恢复提示 / 最近工程列表**。

### 6. 代码架构是「屎山雏形」
- `view.ts` 1300+ 行：手势 / 渲染 / 变换 / 对称全混在里面；
- `App.tsx` 900 行：魔法球 / 停靠 / 菜单 / 设置全塞；
- `session.ts` 是**上帝对象**；
- **UI 层几乎零测试**，仅引擎少量单测；改一个功能牵一发动全身，长期维护成本高。

### 7. 细节糙
- 大量操作**静默失败**（空状态 toast 是后补的）；
- 无障碍 / 可访问性基本没有（ARIA、小屏适配、减少动效）；
- 单文档、无 Tab。

---

## 四、一句话总结

**PixelCraft 在「把 Aseprite 的像素算法搬到触屏」这件事上做得很好（笔刷 / 椭圆 / 对称甚至反超），但在「成为一个能替代 Aseprite / Resprite 的完整工具」上，卡在五个位置：性能、互操作、桌面体验、生态（脚本 / tilemap）、代码可维护性。**

当前更像 **移动端轻量自用 / 移植作品**；要「能打」还需补齐：

---

## 五、改进优先级（建议路线）

| 优先级 | 事项 | 价值 |
|---|---|---|
| 🔥 高 | ① 绘画渲染增量优化（脏矩形 / 增量缓存 / rAF 节流 / 笔迹画到 overlay）——**脏矩形与 rAF 已完成，overlay 层待做** | 大画布不卡，全局手感 |
| 🔥 高 | ② 键盘快捷键（Ctrl+Z/Y、工具切换、保存、空格平移）+ 桌面 UI | 网页端立刻好用 |
| 🔥 高 | ③ `.aseprite` / 标准格式读写 | 打破生态锁死 |
| ⚖️ 中 | ④ 帧标签 / 循环区间、图层成组（多选与拖拽重排已完成） | 编缉效率 |
| ⚖️ 中 | ⑤ 调色板量化 / 降色 / 图像取色板 | 专业像素画 |
| ⚖️ 中 | ⑥ 数据安全：文件级备份、超出容量提示、崩溃恢复 | 可信度 |
| ✅ | ⑦ 新手引导（41 步，含真操作演示）、空状态 toast、设置搜索/导入导出 | 已完成 |

> 已落地的其它条目：空状态 toast、缩放指示 + 适配视图、保留视口、边缘自动平移（可开关、限速）、对称线锁定按钮远离画布、停靠区布局持久化、洋葱皮循环环绕、帧多选、导出帧范围、调色板整理、返回手势二次确认等。
