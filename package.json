{
  "name": "pixelcraft-web",
  "version": "1.0.2",
  "private": true,
  "description": "PixelCraft (像素工坊) — web app sources, type-check, tests and web-bundle build",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "sh scripts/run-tests.sh",
    "build": "sh scripts/build-web.sh",
    "demo": "sh scripts/build-ui-demo.sh"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.25.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "ts-prune": "^0.10.3",
    "typescript": "^5.6.0"
  }
}
